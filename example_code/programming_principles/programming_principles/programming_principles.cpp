// programming_principles.cpp : This file contains the 'main' function. Program execution begins and ends there.
//

#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
#include <cmath>
using namespace std;
inline void keep_window_open() { char ch; cin >> ch; }

int main()
{
    //std::cout << "Hello World!\n";
    //std::cout << "Please enter your first name (followed by 'enter'):\n";
    //string first_name;      //first_name is a variable of type string
    //cin >> first_name;      //read characters into first_name
    //cout << "Hello, " << first_name << "!\n";

    //the input operation >>("get from") is sensitive to type.

    //cout << "Please enter your first name an age\n";
    //string first_name = "???";     // string variable, ??? means don't know the name
    //int age = -1;                // integer variable, -1 means don't know  the age.
    //cin >> first_name >> age;      //read a string
    //cout << "Hello, " << first_name << "(age" << age << ")\n";

    //cout << "Please enter your first and second names\n";
    //string first;
    //string second;
    //cin >> first >> second;     //read two strings
    //cout <<"Hello, "<<first<<" "<<second<<'\n';

    //3.4 Operations and Operands

    //int count;
    //cin >> count;       //reads an integer into count
    //string name;
    //cin >> name;        //reads an integer into name
    //int c2 = count + 2;     //adds integers
    //string s2 = name + "Jr.";   //appends characters

    //int c3 = count - 2;
                                //no subtractions symbol for characters

    /*cout << "Please enter a floating-point value: ";
    double n;
    cin >> n;
    cout << "n==" << n
        << "\n n+1 == " << n + 1
        << "\n three times n == " << 3 * n
        << "\n twice n == " << n * n
        << "\n n wquared == " << n * n
        << "\n half of n ==" << n / 2
        << "\n square root of n == " << sqrt(n)
        << '\n';*/ //another name for newline ("end of line") in output

    //Initialization (giving a variable its initial value)
    //Assignment (giving a variable a new value)
}

// Run program: Ctrl + F5 or Debug > Start Without Debugging menu
// Debug program: F5 or Debug > Start Debugging menu

// Tips for Getting Started: 
//   1. Use the Solution Explorer window to add/manage files
//   2. Use the Team Explorer window to connect to source control
//   3. Use the Output window to see build output and other messages
//   4. Use the Error List window to view errors
//   5. Go to Project > Add New Item to create new code files, or Project > Add Existing Item to add existing code files to the project
//   6. In the future, to open this project again, go to File > Open > Project and select the .sln file
